# Pipeline packet fixtures

Offline, schema-shaped HTTP replay fixtures for all 37 Phase 2 pipeline
operations. They are synthetic and never make network calls; replace them with
tenant recordings when available.
