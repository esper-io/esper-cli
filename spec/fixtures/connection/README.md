Synthetic offline connection replay fixtures for the two packet operations.
The directory contains two success responses, two JSON output goldens, and two
API-error responses. Re-record only with an authorized tenant.
