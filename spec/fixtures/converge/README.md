# Converge packet fixtures

All JSON fixtures in this directory are synthesized from the declared OpenAPI
response schemas. They are committed local test data only and must be
re-recorded against a tenant before they are used as captured API examples.
