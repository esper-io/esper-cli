# Policy Packet Fixtures

These synthesized fixtures match the legacy enterprise-policy response schemas used by packet 20. Re-record them against an enterprise before using them as captured API examples.

The list second page completes the root limit-offset pagination flow. `NEXT_PAGE` is replaced with the test server URL at runtime.
