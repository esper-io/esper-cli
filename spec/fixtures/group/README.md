# Group packet fixtures

All fixtures in this directory are synthesized from the declared OpenAPI response
schemas. They are local test data only and must be re-recorded against a tenant
before they are used as captured API examples.

`RERECORD_WITH_TENANT` is the marker for the required supervised tenant capture.
