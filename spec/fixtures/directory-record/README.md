# Directory Record Packet Fixtures

These synthesized fixtures match the v1 DirectoryRecord response schemas used by packet 16. Re-record them against an enterprise before using them as captured API examples.

The list second page completes the limit-offset pagination flow. `NEXT_PAGE` is replaced with the test server URL at runtime. Delete success is intentionally empty because the endpoint returns bodyless 204.
