# Blueprint packet fixtures

Offline, schema-shaped HTTP replay fixtures for the 15 Phase 2 blueprint
operations. Names are operation-derived. They are synthetic and never make
network calls; replace them with tenant recordings when available.
