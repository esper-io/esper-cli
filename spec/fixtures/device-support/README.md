# Device Support Packet Fixtures

These synthesized fixtures match the legacy, v0, and v2 response schemas used by packet 15, including the public v2 device-state update. Re-record them against a tenant before using them as captured API examples.

The five `*-second-page.json` files complete the pagination flows. `NEXT_PAGE` is replaced with the test server URL at runtime.
