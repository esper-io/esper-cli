# Tile UI Packet Fixtures

These synthesized fixtures match the v1 wallpaper and tile-icon response schemas used by packet 25. Re-record them against an enterprise before using them as captured API examples.

The list second pages complete the limit-offset pagination flows. `NEXT_PAGE` is replaced with the test server URL at runtime.
