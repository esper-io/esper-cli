# Enterprise packet fixtures

All JSON fixtures in this directory are synthesized from the corresponding
OpenAPI response schemas. They are committed local test data only and must be
re-recorded against a tenant before they are used as captured API examples.

`enterprise-policy-delete-success.body` represents the empty HTTP body required
by the operation's `204 No Content` response. It is intentionally empty because
that response has no JSON schema.
